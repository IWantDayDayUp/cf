#include <bits/stdc++.h>

using namespace std;

void solve() {
    int n;
    cin >> n;

    map<int, vector<>> mp;
    for (int i = 1; i <= n; i++)
    {
        int l, r, c;
        cin >> l >> r >> c;


    }
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);

    int t;
    cin >> t;
    while (t--) {
        solve();
    }

    return 0;
}