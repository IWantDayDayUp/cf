#include <bits/stdc++.h>

using namespace std;

using i64 = long long;

void slove() {
    int n, c, q;
    cin >> n >> c >> q;

    string s;
    cin >> s;

    
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);

    int t;
    cin >> t;
    while (t--) {
        slove();
    }

    return 0;
}