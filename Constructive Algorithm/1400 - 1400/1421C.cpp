#include <bits/stdc++.h>

using namespace std;

using i64 = long long;

void slove() {
    string s;
    cin >> s;

    cout << 3 << endl;
    cout << "R" << ' ' << s.size() - 1 << endl;
    cout << "L" << ' ' << s.size() << endl;
    cout << "L" << ' ' << 2 << endl;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);

    slove();

    return 0;
}