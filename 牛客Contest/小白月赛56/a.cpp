#include <bits/stdc++.h>

using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);

    long long a, b, n;
    cin >> a >> b >> n;

    cout << (1 + 0) * n << ' ' << (a + b) * n << endl;

    return 0;
}